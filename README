<h1>HTML 5 Bootstrap!</h1>
<p>This is the result of what would happen if <a href="http://html5boilerplate.com/">H5BP</a>, complete with <a href="https://github.com/h5bp/ant-build-script">build script</a>, had a bab with <a href="http://twitter.github.com/bootstrap/">Twitter Bootstrap</a>. This was built using initializr but it is easier to put it into github and <code>git clone</code></p>